import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#3e8ed0",
		},
		secondary: {
			main: "#3273dc",
		},
		error: {
			main: "#ff3860",
		},
	},
});

export default theme;
